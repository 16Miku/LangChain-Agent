# V10 用户操作测试指南

> 本指南描述启动服务后，在前端界面进行的操作和预期效果

---

## 一、启动服务

### 1.1 启动命令

```bash
# 终端 1: 前端
cd frontend-next && npm run dev

# 终端 2-6: 后端服务
cd backend/auth-service && uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
cd backend/chat-service && uvicorn app.main:app --host 0.0.0.0 --port 8002 --reload
cd backend/rag-service && uvicorn app.main:app --host 0.0.0.0 --port 8004 --reload
cd backend/presentation-service && uvicorn app.main:app --host 0.0.0.0 --port 8005 --reload
cd backend/pptx-renderer-service && uvicorn app.main:app --host 0.0.0.0 --port 8006 --reload
```

### 1.2 访问地址

打开浏览器访问: **http://localhost:3000**

---

## 二、用户认证测试

### 测试 1: 注册新用户（完成）

| 步骤 | 操作 | 预期效果 |
|------|------|----------|
| 1 | 点击页面右上角「注册」按钮 | 弹出注册表单 |
| 2 | 输入用户名: `testuser` | 输入框显示内容 |
| 3 | 输入邮箱: `test@example.com` | 输入框显示内容 |
| 4 | 输入密码: `Test123456` | 密码以圆点显示 |
| 5 | 点击「注册」按钮 | 显示"注册成功"提示，自动跳转到登录页 |

### 测试 2: 用户登录（完成）

| 步骤 | 操作 | 预期效果 |
|------|------|----------|
| 1 | 输入邮箱: `test@example.com` | 输入框显示内容 |
| 2 | 输入密码: `Test123456` | 密码以圆点显示 |
| 3 | 点击「登录」按钮 | 显示"登录成功"，跳转到聊天主页面 |
| 4 | 查看页面右上角 | 显示用户名 `testuser` |

---

## 三、聊天功能测试

### 测试 3: 基础对话（完成）

| 步骤 | 操作 | 预期效果 |
|------|------|----------|
| 1 | 在输入框输入: `你好` | 输入框显示文字 |
| 2 | 点击发送按钮或按 Enter | 消息出现在对话区域 |
| 3 | 等待 AI 响应 | AI 回复逐字流式显示（打字机效果） |
| 4 | 响应完成 | 完整回复显示，包含问候语 |

### 测试 4: 多轮对话（V10 查询改写功能）（完成）

| 步骤 | 操作 | 预期效果 |
|------|------|----------|
| 1 | 发送: `RAG 是什么技术？` | AI 详细解释 RAG 技术 |
| 2 | 发送: `它有什么优点？` | AI 理解"它"指 RAG，回答 RAG 的优点 |
| 3 | 发送: `具体怎么实现的？` | AI 继续基于上下文回答实现方式 |
| 4 | 发送: `刚才说的第一个优点能详细说说吗？` | AI 理解"刚才说的"，定位到具体内容回答 |

**验证点**: AI 能正确理解"它"、"刚才说的"等指代词，无需用户重复说明主题。

### 测试 5: 工具调用（完成）

| 步骤 | 操作 | 预期效果 |
|------|------|----------|
| 1 | 发送: `帮我搜索最新的人工智能新闻` | 显示"正在搜索..."状态 |(完成)
| 2 | 等待工具执行 | 显示搜索结果摘要 |
| 3 | 发送: `用 Python 写一个快速排序` | 显示代码块，语法高亮 |

---

## 四、演示文稿功能测试 (已跳过)

> **注意**: 演示文稿功能测试已跳过，保留测试用例供后续参考。

### 测试 6: 创建演示文稿 (待测试)

| 步骤 | 操作 | 预期效果 |
|------|------|----------|
| 1 | 点击侧边栏「演示文稿」 | 进入演示文稿页面 |
| 2 | 点击「新建演示文稿」 | 显示创建对话框或输入区 |
| 3 | 输入: `帮我创建一个关于人工智能发展历史的5页PPT` | 显示"正在生成..."状态 |
| 4 | 等待生成完成 | 左侧显示 5 张幻灯片缩略图 |
| 5 | 点击任意缩略图 | 右侧显示该幻灯片大图预览 |

### 测试 7: 编辑演示文稿

| 步骤 | 操作 | 预期效果 |
|------|------|----------|
| 1 | 选中第 2 张幻灯片 | 幻灯片高亮显示 |
| 2 | 在对话框输入: `把标题改成"AI的黄金时代"` | 第 2 张幻灯片标题更新 |
| 3 | 输入: `添加一张关于深度学习的幻灯片` | 新幻灯片插入，缩略图列表更新 |
| 4 | 输入: `删除最后一张` | 最后一张幻灯片被删除 |

### 测试 8: 切换主题

| 步骤 | 操作 | 预期效果 |
|------|------|----------|
| 1 | 点击「主题」下拉菜单 | 显示主题列表（如：专业蓝、科技黑、简约白等） |
| 2 | 选择「科技黑」主题 | 所有幻灯片背景变为深色，文字变为浅色 |
| 3 | 选择「简约白」主题 | 所有幻灯片恢复浅色背景 |

### 测试 9: 导出功能

| 步骤 | 操作 | 预期效果 |
|------|------|----------|
| 1 | 点击「导出」按钮 | 显示导出选项（HTML / PPTX） |
| 2 | 选择「导出 HTML」 | 浏览器下载 `.html` 文件 |
| 3 | 打开下载的 HTML 文件 | 在浏览器中显示可演示的幻灯片 |
| 4 | 选择「导出 PPTX」 | 浏览器下载 `.pptx` 文件 |
| 5 | 用 PowerPoint/WPS 打开 | 正常显示所有幻灯片内容 |

---

## 五、RAG 文档问答测试

### 测试 10: 上传文档（完成）

| 步骤 | 操作 | 预期效果 |
|------|------|----------|
| 1 | 点击侧边栏「文档」或「知识库」 | 进入文档管理页面 |
| 2 | 点击「上传文档」按钮 | 弹出文件选择对话框 |
| 3 | 选择一个 PDF 文件（如技术文档） | 显示上传进度条 |
| 4 | 等待解析完成（MinerU 智能解析） | 显示"解析完成"，文档出现在列表中 |

**注意**: V10 集成了 MinerU API，支持智能文档解析（OCR、表格、公式识别）。

### 测试 11: 基于文档问答（完成）

| 步骤 | 操作 | 预期效果 |
|------|------|----------|
| 1 | 返回聊天页面 | 显示聊天界面 |
| 2 | 发送: `根据我上传的文档，总结一下主要内容` | AI 基于文档内容生成摘要 |
| 3 | 查看回复 | 回复中包含「引用来源」标记 |
| 4 | 点击引用来源 | 显示原文出处（页码或段落） |

---

## 六、V10 新功能验证点

### 6.1 查询改写效果验证

在多轮对话中，观察以下场景：

| 用户输入 | AI 应该理解为 |
|---------|--------------|
| `它怎么工作的？` | 上文提到的技术/概念怎么工作 |
| `这个方法有什么缺点？` | 上文提到的方法的缺点 |
| `刚才说的第二点` | 上文列举的第二个要点 |
| `能再详细说说吗？` | 对上一个回答进行展开 |

### 6.2 响应速度验证

| 场景 | V9 预期 | V10 预期（并行优化后） |
|------|---------|----------------------|
| 单工具调用 | ~2秒 | ~2秒（无变化） |
| 多工具调用（如搜索+总结） | ~5秒（串行） | ~3秒（并行执行） |
| 长对话（20轮以上） | 可能变慢 | 保持稳定（上下文压缩） |

### 6.3 PPTX 导出质量验证

导出的 PPTX 文件应满足：

| 检查项 | 预期效果 |
|--------|----------|
| 文字清晰 | 无模糊、无乱码 |
| 布局合理 | 元素不重叠、不溢出 |
| 主题一致 | 颜色、字体与预览一致 |
| 可编辑 | 在 PowerPoint 中可修改文字 |

---

## 七、测试检查清单

### 用户认证
- [x] 注册新用户成功 ✅
- [x] 登录成功，显示用户名 ✅
- [ ] 退出登录后需重新登录

### 聊天功能
- [x] 发送消息，收到流式回复 ✅
- [x] 多轮对话中 AI 理解指代词（它、这个、刚才） ✅
- [x] 工具调用正常执行（搜索、代码执行） ✅
- [x] E2B 代码沙箱正常执行 ✅

### 演示文稿 (跳过测试)
- [ ] 创建 PPT 成功，显示缩略图
- [ ] 对话式修改内容生效
- [ ] 主题切换正常
- [ ] 导出 HTML 可在浏览器打开
- [ ] 导出 PPTX 可在 PowerPoint 打开

### 文档问答 (完成)
- [x] 上传文档成功 (MinerU 智能解析) ✅
- [x] 基于文档问答返回相关内容 ✅
- [x] RAG 检索正常工作 ✅
- [x] 引用来源显示完整内容和位置信息 ✅

---

## 八、常见问题

| 问题 | 可能原因 | 解决方法 |
|------|---------|----------|
| 页面空白 | 前端未启动 | 运行 `npm run dev` |
| 登录失败 | auth-service 未启动 | 检查 8001 端口服务 |
| 消息无响应 | chat-service 未启动 | 检查 8002 端口服务 |
| PPT 生成失败 | presentation-service 未启动 | 检查 8005 端口服务 |
| PPTX 导出失败 | pptx-renderer 未启动 | 检查 8006 端口服务 |
| 文档上传失败 | rag-service 未启动 | 检查 8004 端口服务 |

---

## 九、MinerU API 配置说明

### 9.1 获取 API Key

1. 访问 [MinerU 官网](https://mineru.net)
2. 注册账号并登录
3. 在「API 管理」页面申请 API Key

### 9.2 配置 rag-service

编辑 `backend/rag-service/.env`:

```bash
# MinerU API (智能文档解析)
MINERU_API_KEY=your-api-key-here
MINERU_API_URL=https://mineru.net/api/v4
MINERU_ENABLED=true
```

### 9.3 常见问题

| 问题 | 原因 | 解决方法 |
|------|------|----------|
| 404 错误 | API URL 错误 | 使用 `https://mineru.net/api/v4` |
| 代理拦截 | Clash 等代理软件 | 代码已自动绕过代理 |
| Token 过期 | API Key 过期 | 重新申请 API Key |

---

> **文档版本**: 5.0
> **最后更新**: 2026-02-19
> **适用版本**: V10.0
>
> **测试进度**: 8/8 核心功能已测试通过 ✅
> - ✅ 用户认证 (注册/登录)
> - ✅ 基础对话
> - ✅ 多轮对话 (查询改写)
> - ✅ 工具调用 (网络搜索)
> - ✅ E2B 代码执行
> - ✅ RAG 文档问答 (MinerU)
> - ✅ RAG 引用来源 UI 优化
> - ⏭️ 演示文稿 (跳过)
